{
  "name": "@cg/web",
  "version": "0.4.0",
  "private": true,
  "description": "Control-plane dashboard: devices, connections, permissions, approvals, audit. On Vercel it also mounts the gateway edge (docs/20).",
  "//@cg/gateway": "Dependency, not a copy: this app mounts the gateway's ./edge export on its own routes. apps/gateway/src/main.ts — the only Bun-coupled module — is deliberately not exported, so no Bun global can enter the Next build.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "vitest run"
  },
  "dependencies": {
    "@auth/core": "0.41.3",
    "@cg/adapter-blender": "workspace:*",
    "@cg/adapter-remote-mcp": "workspace:*",
    "@cg/auth": "workspace:*",
    "@cg/core": "workspace:*",
    "@cg/executor": "workspace:*",
    "@cg/gateway": "workspace:*",
    "@cg/observability": "workspace:*",
    "@cg/policy": "workspace:*",
    "@cg/protocol": "workspace:*",
    "@cg/registry": "workspace:*",
    "@cg/schemas": "workspace:*",
    "@convex-dev/auth": "0.0.95",
    "convex": "1.44.0",
    "next": "16.3.1",
    "next-themes": "0.4.6",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@edge-runtime/vm": "5.0.0",
    "@tailwindcss/postcss": "4.3.3",
    "@types/node": "22.19.2",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "convex-test": "0.0.55",
    "tailwindcss": "4.3.3",
    "typescript": "5.9.3",
    "vitest": "3.2.7"
  }
}
