# packages/registry

Connector discovery and version registry.

Owns:

- built-in connector registration;
- manifest validation;
- action lookup;
- connector version metadata;
- capability resolution.

MVP should use a trusted built-in registry. Third-party publishing can come later.
