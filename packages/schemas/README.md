# packages/schemas

JSON Schemas and validation artifacts for contracts shared across services.

Start with:

- connector manifest;
- action definition;
- device capability announcement;
- job envelope;
- result envelope.

Schemas are versioned contracts, not informal TypeScript-only shapes.
